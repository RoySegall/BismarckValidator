meta:
  sheet: "ic"

fields:
    instrument_id:
      - func: "not_null"
      - args: {}

    currency:
      - func: "valid_currency"
      - args: {}

    date_of_purchase:
      - func: "date_format"
      - args: {
          format_to_validate: "%d/%m/%Y",
          format_to_display: "DD/MM/YYYY"
          }

