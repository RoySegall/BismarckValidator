# Listing the list of fields.
meta:
  sheet: "cash"

fields:

  currency:
    - func: "valid_currency"
      args: {}

  issuer_id:
    - func: "is_numeric"
      args: {}

  rate_of_fund:
    - func: "is_numeric"
      args: {}
    
    - func: "decimal_positive"
      args: {}